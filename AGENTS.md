# Adam Pugh site

- The active site is `site/`; `archive/` is preserved legacy material.
- Do not invent personal history, portfolio claims, quotations, or credentials. Keep source support for factual claims.
- Verified site commands: from `site/`, `npm run start`, `npm run build`, and `npm run serve`.
